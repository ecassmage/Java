public interface NumberFormatter {
    /**
     * A format which will change the integer into some formatted String to be returned
     * @param n  Takes an integer
     * @return  Returns a String
     */
    String format(int n);
}
